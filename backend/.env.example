# Backend environment variables (copy to backend/.env and fill in)

# OpenAI API key — required for chat, identify, and TTS endpoints
OPENAI_API_KEY=""

# Production hardening (Railway)
# DEBUG=False
# SECRET_KEY="your-secure-secret-key"
# ALLOWED_HOSTS="<your-railway-domain>.railway.app,localhost,127.0.0.1"
